#include "libft.h"

//copies n bytes from src to dest. The memory areas may overlap: copying takes place as though the bytes in src are forst copies into a temporary array that does not overlap src or dest, and the bytes than copies from temporary array to dest. 

void	*ft_memmove(void *dest, const void *src, size_t n)
{
	*dest[] = *src[];







	return (*dest);
}
